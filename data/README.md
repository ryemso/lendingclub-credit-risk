# Data

원본 LendingClub 대출 데이터는 저장소에 포함하지 않습니다.

실행 시 아래 경로에 CSV를 배치합니다.

```text
data/accepted_2007_to_2018Q4.csv
```

`src/modeling_pipeline.py`는 원본 notebook에서 사용한 주요 컬럼만 읽도록 구성되어 있습니다.

데이터 라이선스와 배포 조건은 원본 데이터 제공처의 정책을 따릅니다.
